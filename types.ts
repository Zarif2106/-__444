export interface CardProps {
  id: number;
  name: string;
  price: string;
  description: string;
  image: string;
  link: string;
}

export interface ProductData {
  id: number;
  title: string;
  body: string;
}